# 自定义主题编写指南

本编辑器的主题系统参照 velotype 的主题接口：主题是一个 **JSON/JSONC 或 YAML** 文件（按内容自动识别），导入后立即生效，并持久化保存在本机（localStorage）。

## 主题覆盖范围

主题可定制应用的**全部**视觉部分：

- 编辑器全部块类型（段落/标题/列表/任务/引用/callout/表格/代码块/公式/Mermaid/脚注/分割线/注释/图片/图文排版块）——`colors` token + `blocks` 按块样式
- 行内样式（链接/行内代码/粗斜体/删除线/==高亮==/kbd/上下标/脚注引用/列表标记/勾选框）
- 代码语法高亮（`code_syntax_*` 十个 token）
- 侧边栏（标签页、指示条）、状态栏（背景/文字/悬停/指示色）、标题栏、菜单（/ 菜单、右键菜单、表格面板，均经 `--t-*` 变量与 `blocks` 界面键定制）
- 滚动条、光标、选区、排版（字号/字重/字体/行高）、尺寸（边距/列宽/圆角）、公式中文回落字体
- 斜杠命令菜单（`blocks.slashMenu` / `slashMenuItemActive`）

仅图标的多彩品牌色（功能图标配色）为固定设计，不随主题变化。

## 快速开始

1. 复制 `assets/custom-theme.example.jsonc`（JSONC）或 `assets/themes/eye-care-green.yaml`（YAML）为新文件。
2. 填写 `name`，并只保留你想覆盖的字段（其余删掉或留 `null`）。
3. 点击状态栏右下角的主题下拉，选择「导入主题…」，选中该文件（支持 `.json/.jsonc/.yaml/.yml`）。
4. 主题立即应用并出现在主题下拉里，之后可随时切换。

## YAML 格式

```yaml
name: 我的主题        # 必填
creator: your-name
baseThemeId: velotype-light   # 可选：缺失字段继承的内置主题（velotype 暗色 / velotype-light 亮色）
theme:
  colors:
    editor_background: '#f0f7e8'
    text_default: '#2d3a24'
  typography:
    text_size: 16
    text_line_height: 1.7
  dimensions:
    content_max_width: 760
  blocks:
    heading:
      h1:
        borderBottom: 2px solid #c9dbaf
```

规则：

- 嵌套映射用**空格缩进**（不支持 Tab）；`# ` 后为注释（颜色 `#ff0000` 后无空格，不受影响）。
- 字符串可不加引号；含特殊字符建议用单/双引号。数字、布尔、`null`/`~` 按字面解析。
- 不支持列表、flow 语法（`{...}`/`[...]`）、多行字符串与锚点——主题包结构仅为嵌套映射。
- `null` 与不写字段同样继承基主题；键名与 JSON 格式完全一致。

## 文件格式（JSON/JSONC）

```jsonc
{
  // 必填：主题显示名（状态栏主题菜单中展示）
  "name": "我的主题",
  // 可选：作者
  "creator": "your-name",
  // 可选：缺失字段继承的内置主题
  // "velotype"（暗色）或 "velotype-light"（亮色）；缺省继承当前主题的内置基主题
  "baseThemeId": "velotype-light",
  // 可选元数据（仅记录，不影响渲染）
  "description": "",
  "version": "1.0.0",
  "homepage": "",
  "license": "",

  "theme": {
    "colors":     { /* 颜色 token，任意 CSS 颜色值 */ },
    "typography": { /* 字号/字重 token */ },
    "dimensions": { /* 尺寸 token，单位 px */ }
  }
}
```

规则：

- 支持 JSONC：`//` 与 `/* */` 注释均可使用（字符串内的 `//` 不受影响）。
- `null` 与空字符串 `""` 视为「未设置」，继承基主题；未写的字段同样继承。
- 内置基主题：`velotype`（暗色，Mdmdt Dark）、`velotype-light`（亮色，Mdmdt Light），配色参考 [Mdmdt](https://github.com/cayxc/Mdmdt)。
- 颜色值：任意合法 CSS 颜色（`#rgb`、`#rrggbb`、`#rrggbbaa`、`rgb()/rgba()`、具名色）。

## 可用 token

### colors（颜色）

| token | 作用 |
|---|---|
| `editor_background` | 编辑区背景（兼作应用外壳背景） |
| `source_mode_block_bg` | 编辑中块的背景 |
| `comment_bg` | 可见注释块背景 |
| `text_default` | 正文颜色 |
| `text_link` | 链接、脚注引用颜色 |
| `text_placeholder` | 弱化文字（占位提示、列表标记、脚注定义） |
| `text_h1` ~ `text_h6` | 各级标题颜色 |
| `text_quote` | 引用块文字 |
| `border_quote` | 引用块左边框 |
| `callout_note_bg` / `callout_note_border` | NOTE callout 背景 / 边框与标签 |
| `callout_tip_bg` / `callout_tip_border` | TIP callout |
| `callout_important_bg` / `callout_important_border` | IMPORTANT callout |
| `callout_warning_bg` / `callout_warning_border` | WARNING callout |
| `callout_caution_bg` / `callout_caution_border` | CAUTION callout |
| `footnote_badge_text` | 脚注定义文字 |
| `separator_color` | 分割线颜色 |
| `code_bg` | 行内代码与代码块背景 |
| `code_text` | 代码文字 |
| `text_strong` | 粗体文字颜色 |
| `text_em` | 斜体文字颜色 |
| `text_strikethrough` | 删除线文字颜色 |
| `mark_bg` | `==高亮==` 标记背景色 |
| `mark_text` | `==高亮==` 标记文字颜色 |
| `kbd_bg` | `<kbd>` 键盘按键背景 |
| `kbd_text` | `<kbd>` 键盘按键文字 |
| `kbd_border` | `<kbd>` 键盘按键边框 |
| `list_marker` | 列表标记（•、序号）颜色 |
| `table_header_text` | 表头文字颜色 |
| `task_checkbox_border` / `task_checkbox_bg` | 任务勾选框边框 / 未勾选背景 |
| `task_checkbox_checked_bg` / `task_checkbox_check` | 勾选后背景 / 勾选标记颜色 |
| `scrollbar_thumb` | 滚动条滑块 |
| `scrollbar_track` | 滚动条轨道（默认 `transparent`） |
| `code_syntax_comment` | 语法高亮：注释 |
| `code_syntax_keyword` | 语法高亮：关键字 |
| `code_syntax_string` | 语法高亮：字符串 |
| `code_syntax_number` | 语法高亮：数字 |
| `code_syntax_type` | 语法高亮：类型/类名 |
| `code_syntax_function` | 语法高亮：函数名 |
| `code_syntax_constant` | 语法高亮：常量/内建 |
| `code_syntax_variable` | 语法高亮：变量 |
| `code_syntax_property` | 语法高亮：属性/字段 |
| `code_syntax_operator` | 语法高亮：运算符 |
| `code_syntax_punctuation` | 语法高亮：标点 |
| `table_border` | 表格边框（兼作侧栏/状态栏等通用边框色） |
| `table_header_bg` | 表头背景 |
| `table_cell_bg` | 表格单元格背景 |
| `image_placeholder_bg` / `image_placeholder_border` / `image_placeholder_text` | 数学公式 / Mermaid 占位块 |
| `cursor` | 光标颜色 |
| `selection` | 选区高亮 |
| `status_bar_background` | 状态栏背景（默认融入编辑器背景） |
| `status_bar_border` | 状态栏上边框颜色（默认 `transparent` 无线条） |
| `tab_indicator` | 侧边栏活跃标签底部指示条颜色（仅活跃标签显示，带切换过渡动画） |
| `status_bar_text` | 状态栏文字 |
| `status_bar_text_dim` | 状态栏弱化文字 |
| `status_bar_button_hover` | 状态栏按钮悬停（兼作标题栏/侧栏通用悬停色） |
| `quote_bg` | 引用块背景（Mdmdt 扩展 token，`blocks.quote` 引用） |
| `inline_code_bg` / `inline_code_text` | 行内代码背景 / 文字（Mdmdt 扩展，`blocks.inlineCode` 引用） |
| `link_hover` | 链接悬停颜色（Mdmdt 扩展，`blocks.link` 的 `:hover` 引用） |
| `strikethrough_line` | 删除线线条颜色（Mdmdt 扩展，`blocks.strikethrough` 引用） |
| `table_zebra_bg` | 表格偶数行斑马纹背景（Mdmdt 扩展，`blocks.table` 的后代选择器引用） |

上表最后 6 项是内置 Mdmdt 主题新增的扩展 token：`colors` 可自由新增任意键（自动导出为同名
`--t-*` CSS 变量），配合 `blocks` 引用即可实现主题专属样式，自定义主题可同法扩展。

### typography（排版）

| token | 作用 | 取值 |
|---|---|---|
| `font_family` | 正文字体栈 | CSS font-family（逗号分隔栈） |
| `code_font_family` | 代码字体栈 | CSS font-family |
| `text_size` | 正文字号 | 数值（px） |
| `text_line_height` | 正文行高 | 倍数（如 `1.6`） |
| `h1_size` ~ `h6_size` | 各级标题字号 | 数值（px） |
| `h1_weight` ~ `h6_weight` | 各级标题字重 | `thin`/`light`/`normal`/`medium`/`semibold`/`bold`/`extrabold`/`black` |
| `code_size` | 代码字号 | 数值（px） |
| `script_scale` | 上下标字号比例 | 倍数（如 `0.75`） |
| `math_cjk_font` | 公式中文回落字体 | 字体文件路径（可加 `#索引` 或 `#字体族名`）；运行中切换需重启应用生效 |

### dimensions（尺寸）

| token | 作用 | 取值 |
|---|---|---|
| `editor_padding` | 编辑区内边距 | 数值（px） |
| `content_max_width` | 内容列最大宽度（居中排版） | 数值（px）；`null` 为全宽 |
| `quote_border_width` | 引用块左边框宽度 | 数值（px） |
| `callout_radius` | callout 圆角 | 数值（px） |
| `code_bg_radius` | 行内代码圆角 | 数值（px） |
| `scrollbar_width` | 滚动条宽度 | 数值（px，默认 8） |

### blocks（按块类型自定义样式）

`blocks` 以**块类型**为键，值为任意 CSS 属性表，作用于该类型块的根元素（渲染态与编辑态同时生效）：

```jsonc
"blocks": {
  // 所有段落
  "paragraph": { "letterSpacing": "0.02em" },
  // 所有标题共用属性 + h1~h6 分级覆盖
  "heading": {
    "color": "#b45309",
    "h1": { "borderBottom": "2px solid", "paddingBottom": 4 },
    "h3": { "fontStyle": "italic" }
  },
  "quote":     { "background": "#00000008", "borderRadius": 6, "padding": "4px 12px" },
  "codeBlock": { "borderRadius": 10, "border": "1px solid #00000014" },
  "table":     { "fontSize": 13 },
  "mathBlock": { "textAlign": "center" }
}
```

规则：

- 可选块类型键：`paragraph`、`heading`、`separator`、`bulletedListItem`、`taskListItem`、`numberedListItem`、`quote`、`callout`、`footnoteDefinition`、`table`、`codeBlock`、`comment`、`htmlBlock`、`sectionBlock`、`mathBlock`、`mermaidBlock`、`plantumlBlock`、`rawMarkdown`。内置主题为 `mermaidBlock`/`plantumlBlock` 预置了滑过浮起动画（`transition` + `:hover` 位移与阴影），可在自定义主题中覆盖：
  ```jsonc
  "mermaidBlock": {
    "transition": "transform 0.25s ease, box-shadow 0.25s ease",
    ":hover": { "transform": "translateY(-2px)", "boxShadow": "0 8px 24px #00000059" }
  }
  ```
- `sectionBlock` 为 `<section>` 图文排版块（Mdmdt 式 grid 布局），其根选择器为 `.blk-section-block`，内部 `<section>` 是唯一直接子元素——用后代键 `"> section"` 可覆盖 grid 间距/对齐等：
  ```jsonc
  "sectionBlock": { "> section": { "gridGap": "24px", "alignItems": "start" } }
  ```
- 另支持行内键：`link`（链接）、`inlineCode`（行内代码）、`strong`、`em`、`strikethrough`、`mark`（==高亮== 标记）、`kbd`（键盘按键）、`superscript`（上标）、`subscript`（下标）、`footnoteRef`（脚注引用）、`listMarker`（列表标记）、`taskCheckbox`（任务勾选框）。
- 界面区域键：`sidebarTab`（侧边栏标签，可定制悬停背景、过渡动画）、`sidebarTabActive`（活跃标签，可定制指示条颜色/粗细/样式）、`slashMenu`（斜杠命令菜单，可定制背景/边框/圆角/阴影）、`slashMenuItemActive`（菜单选中项背景）、`mathPreview`（公式/Mermaid/PlantUML 编辑实时预览浮动面板，可定制背景/边框/圆角/阴影）——`blocks` 作用域为整个应用窗口（`.t-app`），覆盖编辑区与侧栏/状态栏。
- **伪类/伪元素**：键以 `:` 开头即追加到该元素选择器之后，支持 `:hover`、`:focus`、`:active`、`:checked`、`::before`、`::after`、`:nth-child(...)` 等任意伪类/伪元素，值为声明表：
  ```jsonc
  "link": { ":hover": { "color": "#fff", "background": "#19c8b9" } },
  "heading": { "h1": { "::after": { "content": "\"\"", "display": "block", "height": "8px" } } }
  ```
- **后代选择器**：键含空白/括号/组合器（`>` `+` `~`）即视为该元素内的后代选择器，可实现斑马纹等：
  ```jsonc
  "table": {
    "tbody tr:nth-child(even) td": { "background": "#efe9d6" },
    "tbody tr:hover td": { "background": "#e6f9f6" }
  }
  ```
- 属性名 camelCase 或 kebab 均可（`borderRadius` / `border-radius`）；数值自动补 `px`（`fontWeight`、`lineHeight`、`opacity` 等无单位属性除外）。
- 只有 `heading` 支持嵌套 `h1`~`h6` 子表做分级覆盖（子表内同样支持伪类/后代键）。
- `blocks` 与 `colors` 叠加时 `blocks` 规则后注入、优先级更高——可用它覆盖内置配色在特定块上的表现（如单独改标题色）。
- 出于安全考虑，属性值中的 `}` 和 `</style`、选择器键中的 `{`/`}`/`<`/`;` 会被忽略（该条规则不生效）；SVG data URI 等含 `<>` 的合法值不受影响。

## 主题样例

- `assets/custom-theme.example.jsonc`：全 token 注释样板（JSONC）
- `assets/themes/eye-care-green.yaml`：YAML 格式示例（护眼绿）
- `assets/themes/animal-island.jsonc`：完整实战主题（移植自 Typora 的 animal-island 主题，涵盖六级标题样式、软枕卡片、脚印分隔线、勾选框、表格、链接等几乎全部定制点，可作为编写参考）

## 完整示例

一个基于亮色主题的「护眼绿」：

```jsonc
{
  "name": "护眼绿",
  "creator": "me",
  "baseThemeId": "velotype-light",
  "theme": {
    "colors": {
      "editor_background": "#f0f7e8",
      "text_default": "#2d3a24",
      "text_link": "#4a7c2f",
      "code_bg": "#e3edd6",
      "status_bar_background": "#dfe9cf",
      "status_bar_button_hover": "#c9dbaf"
    },
    "typography": {
      "text_size": 16,
      "text_line_height": 1.7
    },
    "dimensions": {
      "content_max_width": 760
    },
    "blocks": {
      "heading": { "h1": { "borderBottom": "2px solid #c9dbaf", "paddingBottom": 6 } },
      "codeBlock": { "borderRadius": 10 }
    }
  }
}
```

未覆盖的字段（标题颜色、callout、表格等）全部继承 `velotype-light`。

## 内置主题参考值

编写时可直接参考内置主题的完整取值（即两套主题的「全开」样板）：

- 暗色：`src/themes/velotype.js`
- 亮色：`src/themes/velotype-light.js`

## 注意事项

- 导入的主题保存在 localStorage，清除应用数据会丢失，请保留主题源文件。
- 同名主题重复导入会生成新条目（按导入时间区分），不会覆盖。
- 目前无删除入口；如需清理，可在浏览器 DevTools 中删除 localStorage 键 `tauri-editor.theme.custom`。
